#!/usr/bin/env bash
# Configures an Ubuntu machine to have a custom HTTP response header (X-Served-By)

# Update packages and install Nginx
sudo apt-get update
sudo apt-get install -y nginx

# Create an index file
echo "Hello world!" | sudo tee /var/www/html/index.html

# Configure the custom HTTP response header with the hostname
HOSTNAME=$(hostname)
sudo sed -i "/server_name _;/a \\\n\tadd_header X-Served-By ${HOSTNAME};" /etc/nginx/sites-enabled/default

# Create a custom 404 error page
echo "Ceci n'est pas une page" | sudo tee /var/www/html/404.html

# Configure the 404 error page and location block
sudo sed -i "/listen 80 default_server;/a \\terror_page 404 /404.html;\n\tlocation = /404.html {\n\t\troot /var/www/html;\n\t\tinternal;\n\t}" /etc/nginx/sites-enabled/default

# Restart Nginx to apply changes
sudo systemctl restart nginx
